<html lang='es'>
  <head>
    {{>head}}
  </head>
  <body>
    {{>navbar}}
    {{!-- {{>navbar}} --}}
    <br>
                <div class="row justify-content-md-center">
              <div class="col-md-6">
                <div class="card mt-4 shadow-lg p-3 mb-5 bg-white rounded" style="margin-top: 0px !important">

    <table class="table align-content-center">
    <thead>
      <tr>
        <th scope="col">Usuario</th>
        <th scope="col">Ultimo mensaje</th>

      </tr>
    </thead>
            {{#if chats}}
          {{!-- {{chats}} --}}
        {{#each chats}}
         <tr>
          <td>
            <a href="/admin/chats/{{this.username}}">{{this.username}}</a>
          </td>
          <td>{{this.timestamp}} </td>
        </tr>
        {{/each}}
      {{else}}
      </tbody>
      </table>
        Sin chats
      {{/if}}
  
  </div>
  </div>

</tbody>
</table>

    </div>
  </div>
</div>

    <script src="../index.js"></script>
<script src="/socket.io/socket.io.js"></script>

  </body>
   {{> footer}}
</html>
